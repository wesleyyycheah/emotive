version: 2.1
orbs:
  node: circleci/node@4.3.0
jobs:
  build:
    executor:
      name: node/default
      tag: '14.x'
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
      - run: npm run deploy