import * as React from 'react';
import { useState } from 'react';
import { Text } from 'react-native';
import styled from 'styled-components/native';

type CompProps = {};
const People = (props: CompProps) => {
  return <Text>People</Text>;
};

export default People;
