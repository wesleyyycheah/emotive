import * as React from 'react';
import { useState } from 'react';
import { Text } from 'react-native';
import styled from 'styled-components/native';

type CompProps = {};
const Goals = (props: CompProps) => {
  return <Text>Goals</Text>;
};

export default Goals;
